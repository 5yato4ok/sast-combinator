services:
  context_extractor:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - API_TOKEN=${API_TOKEN}
      - AIST_TOKEN=${AIST_TOKEN}
    restart: unless-stopped
